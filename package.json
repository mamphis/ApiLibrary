{
  "name": "@mamphis/api",
  "workspaces": [
    "api-node",
    "api-vue"
  ],
  "scripts": {
    "dep": "npm ci --workspaces",
    "build": "npm run build --workspaces",
    "updateVersion": "npm version patch --workspaces --git-tag-version=false",
    "publish-lib": "npm run publish-lib --workspaces"
  }
}
